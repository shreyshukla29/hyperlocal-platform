import { jest } from '@jest/globals';

export const uploadImage = jest.fn();
export const deleteImage = jest.fn();
export const extractPublicIdFromUrl = jest.fn();
export const validateImageFile = jest.fn();
export const optimizeImage = jest.fn();
